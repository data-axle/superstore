source "http://rubygems.org"
gemspec

group :test do
  # gem 'cassandra', git: 'git://github.com/twitter/cassandra.git', require: 'cassandra/1.0'
  gem 'cassandra', path: '~/code/cassandra', require: 'cassandra/1.0'
end